#!/usr/bin/env python3

import serial
import socket
import struct
import time


kukaSocket = socket.socket()
host = socket.gethostname()
port = 52323
s.bind((host,port))

s.listen(5)

ser = serial.Serial('/dev/ttyUSB0',9600)

def sendByte(value):
    ser.write(struct.pack('>B',value))
def 

while (True):
    c, addr = s.accept()
    value = c.recv(4)
    intValue = struct.unpack('>i',value))

    ser.write(value)
